# PostgreSQL password
DB_PASSWORD=changeme_use_a_strong_password

# Database connection (for local dotnet ef CLI commands — not used by the app)
ConnectionStrings__Default=Host=localhost;Database=cashout;Username=cashout;Password=changeme_use_a_strong_password
